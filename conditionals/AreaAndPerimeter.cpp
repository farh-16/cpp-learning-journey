#include<iostream>
using namespace std;
int main()
{
    int l;
    cout<<"LENGTH= ";
    cin>>l;
int b;
    cout<<"BREATH= ";
    cin>>b;
// int x=l*b;
// int y=2*(l+b);
if
 (l*b>2*(l+b))
cout<<"Area is greater than perimeter";
else if (l*b<2*(l+b))
cout<<"perimeter is greater than area";
else 
    cout<<"both are equal";

}